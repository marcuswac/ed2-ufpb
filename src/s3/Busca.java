package s3;

public interface Busca {
    /*
     * Busca o elemento x no array a.
     * Se x estiver em a, retorna o seu índice no array.
     * Caso contrário, retorna -1.
     */
    public int busca(int[] a, int x);
}
